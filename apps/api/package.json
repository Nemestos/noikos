{
	"name": "@noikos/api",
	"version": "0.0.1",
	"private": true,
	"description": "Noikos REST API built with Effect and Node.js",
	"type": "module",
	"main": "./dist/index.js",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"check": "biome check ./src",
		"check:fix": "biome check ./src --write",
		"dev": "tsx watch src/index.ts",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@noikos/types": "workspace:*",
		"@noikos/utils": "workspace:*",
		"effect": "^3.21.3"
	},
	"devDependencies": {
		"tsx": "^4.22.4"
	}
}
